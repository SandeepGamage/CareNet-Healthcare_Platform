apiVersion: v1
kind: Service
metadata:
  name: appointment-service
spec:
  selector:
    app: appointment-service
  ports:
    - protocol: TCP
      port: 3004
      targetPort: 3004
  type: NodePort
  ports:
    - protocol: TCP
      port: 3004
      targetPort: 3004
      nodePort: 32004   # (optional, pick a port between 30000-32767)
